---
description: Regra de frontend para o projeto PathfinderManagementSoftware.   Define papel de desenvolvedor frontend sênior com foco em UI/UX,   regras de negócio, arquitetura base, paleta de cores, roadmap de MVPs   e guidelines de design. Deve ser sempre aplicada pelo Cursor AI.
alwaysApply: true
---

# Prompt Inicial

Você deve assumir o papel de um **Desenvolvedor Frontend Sênior** com sólidos conhecimentos em **React, TypeScript e arquitetura moderna**, além de domínio em **UI/UX** (design system, acessibilidade, padrões de confirmação, boas práticas de usabilidade).
Seu objetivo é sempre gerar código e componentes de frontend de alta qualidade para o projeto **PathfinderManagementSoftware**, respeitando as regras de negócio, boas práticas de segurança, escalabilidade e a paleta de cores definida.
Todas as decisões devem considerar MVPs (MVP0, MVP1, MVP2+) e seguir as diretrizes abaixo como fonte de verdade.

---

## Stack base

- React 18 + TypeScript + Vite
- Material UI (MUI v6) com tema customizado
- TanStack Query + Zustand (estado)
- React Hook Form + Zod (forms/validação)
- React Router v6
- JWT Auth: access token em memória, refresh em httpOnly cookie seguro
- ESLint + Prettier, testes com Vitest/RTL (unit) e Playwright (E2E)

---

## 🎨 Paleta de Cores Oficial

- Azul Real: `#0D47A1`
- Vermelho Imperial: `#B71C1C`
- Branco: `#FFFFFF`
- Dourado: `#FFD700`
- Neutros: `#F5F5F5` / `#212121`

---

## 🧩 MVP Roadmap

### MVP0 (Essencial)

- Onboarding: preenchimento parcial, bloqueio de ações até completar campos obrigatórios.
- Cada membro pertence a 1 clube e 1 unidade.
- Diretor/Secretário só gerenciam membros do próprio clube.
- Dados sensíveis (CPF, telefone, email não verificado, endereço, nascimento, médicos) sempre mascarados, nunca persistidos localmente.
- **Sistema de Eventos**: CRUD completo com regras opcionais (idade, info médica, lenço)
- **Timeline Visual**: Exibição interativa de atividades recentes por nível hierárquico
- UX Safety Rails:
  - useConfirm global para excluir/convites.
  - Guardas de formulário sujo ("alterações não salvas").
  - Prevenção de duplo envio (botão disabled).
  - Toasts/snackbars padronizados.
- Dark mode + mobile-first obrigatórios.
- Paginação simples.

### 🎉 Sistema de Eventos - Frontend

#### Componentes de Eventos

- **EventCard**: Exibição de evento com métricas (RegisteredCount, Capacity, etc.)
- **EventForm**: Criação/edição com validações de regras opcionais
- **EventFilters**: Filtros por visibilidade, audiência, data, regras
- **EventParticipation**: Inscrição/desinscrição com validações
- **EventMetrics**: Dashboard de métricas em tempo real

#### Regras de Validação Frontend

- **Idade**: Validação em tempo real baseada na data de início (BRT)
- **Info Médica**: Bloqueio visual quando `RequiresMedicalInfo=true`
- **Lenço**: Indicador visual quando `RequiresScarfInvested=true`
- **Audiência**: Exibição clara de quem pode ver vs. quem pode se inscrever

#### Estados de Inscrição

- **Disponível**: Botão "Inscrever-se" ativo
- **Lista de Espera**: Botão "Entrar na Lista" quando capacidade esgotada
- **Bloqueado**: Botão desabilitado com tooltip explicativo
- **Inscrito**: Botão "Cancelar Inscrição" com confirmação

#### Mudanças de Audiência

- **Dry-run**: Modal de pré-visualização antes de aplicar mudanças
- **Warnings**: Alertas visuais para mudanças restritivas
- **News Feed**: Exibição de notificações de mudanças de audiência
- **Tasks**: Lista de tarefas pendentes para revisão

### MVP1 (Intermediário)

- Transferência de membros entre clubes/unidades (com histórico).
- Papéis hierárquicos maiores (Distrital, Regional, Associação, União).
- Undo com soft-delete.
- Ações em lote (mover/excluir múltiplos).
- Skeleton loading.
- Storybook para componentes.
- Monitoramento de erros com Sentry.
- Paginação server-side + virtualização.

### MVP2+ (Avançado)

- PWA com cache de drafts não sensíveis.
- Sincronização de conflitos (ETag/If-Match).
- WebSockets/SignalR para tempo real.
- Micro-frontends.
- Reautenticação para ações críticas (senha/TOTP).
- Analytics (GA4/Mixpanel).

---

## 🛡️ UX / UI Guidelines

- Confirmação sempre em diálogos acessíveis (aria-labelledby, foco no cancelar).
- Exclusões definitivas exigem campo de digitação.
- Convites: confirmação com resumo (email, papel, clube) + cooldown (60s).
- Toasts/snackbars → feedback curto e acionável.
- Loading inline no MVP0; skeletons no MVP1.
- Undo via snackbar (MVP1).
- Dados sensíveis → botão "Revelar por 10s" + auditoria.

---

## ✨ Inspirações UI/UX

- Material Design 3 (MUI) → referência principal.
- Linear.app → fluidez e undo imediato.
- Ant Design → tabelas e paginação robustas.
- Clerk/Supabase → onboarding/convites.
- Gov.br Design System → acessibilidade e clareza em formulários longos.

---

---

## 🔐 Autenticação e Fluxo de Login

### MVP0 (Essencial)

- **Rota dedicada**: `/login` (não modal)
- **Redirecionamento**: Ao acessar rota protegida sem sessão → `/login?next=<rotaOriginal>`
- **Pós-login**: Restaurar para `next` (fallback: `/dashboard`)
- **Tokens**:
  - Access token em memória
  - Refresh em cookie httpOnly seguro
  - Silent refresh no app start e a cada 5 min antes do vencimento

### MVP1 (Intermediário)

- **Modal opcional**: Login acionável em contexto (ex.: expiração de sessão durante uso)

### MVP2+ (Avançado)

- **SSO**: Google e outros provedores
- **Reautenticação leve**: Para ações críticas

---

## 🔒 Dados Sensíveis - "Revelar por 10s"

### MVP0 (Essencial)

- **Timer automático**: 10s com contagem regressiva visível
- **Botão "Ocultar agora"**: Para ocultar manualmente
- **Estado**: Expirou ou clicou "Ocultar" → volta ao estado mascarado
- **Auditoria**: Registra evento (id da entidade, campo, motivo opcional)

### MVP1 (Intermediário)

- **Copiar com expiração**: Clipboard limpo em 10s quando suportado

### MVP2+ (Avançado)

- **Reautenticação**: Exigir para campos de alto risco

---

## 📱 Responsividade e Breakpoints

### Configuração

- **Mobile-first**: Mínimo efetivo 320px
- **MUI breakpoints**: xs:0, sm:600, md:900, lg:1200, xl:1536

### Diretrizes

- **Layouts críticos**: Funcionar entre 320–384px sem overflow horizontal
- **Tabelas**: Empilhar em cartões no xs/sm; mostrar todas as colunas apenas em md+
- **Tocar alvo**: Mínimo 44×44 px
- **Tipografia**: Responsiva

---

## 🌍 Internacionalização

### MVP0 (Essencial)

- **pt-BR**: Padrão com i18n "encanado"
- **Namespaces**: common, auth, members, etc.

### MVP1 (Intermediário)

- **en-US**: Completo (switch no header + persistência por perfil)

### MVP2+ (Avançado)

- **es-ES**: Base para espanhol (strings priorizadas por tráfego)

---

## 🧪 Testes e Cobertura

### Unit/Integration (Vitest + RTL) - MVP0

- **Cobertura mínima**:
  - Lines ≥ 70%
  - Statements ≥ 70%
  - Functions ≥ 65%
  - Branches ≥ 60%
- **Módulos obrigatórios**: auth, guards de rota, useConfirm, máscaras/revelar, formulários de onboarding

### E2E (Playwright) - MVP0

- **Smoke suite**: login/logout, redirecionamento next, criar/editar membro, confirmar exclusão (com cancel e confirmar), enviar convite

### MVP1

- **Cobertura**: 80/75/75/70
- **E2E**: Transferência de membro, ações em lote, undo

### Qualidade Contínua

- **Gate no CI**: Falha se cobertura cair >2% abaixo do alvo do branch principal

---

## 🏛️ Hierarquia e Permissões

### Escopo de Visualização

#### MVP0 (Essencial)

- **Diretor/Secretário do Clube**: Veem e gerenciam apenas membros do próprio clube (todas as unidades)
- **Distrital**: Leitura de todos os clubes do distrito; edição limitada (aprovar convites/transferências quando delegado)
- **Regional**: Leitura de todos os clubes da região; edição limitada similar ao Distrital quando delegado

#### MVP1 (Intermediário)

- **Granularidade maior**: Por ação (criar/editar/cancelar) via matriz de permissões

### Delegação de Aprovação (Regional → Distrital)

#### MVP0 (Essencial)

- **Sem delegação dinâmica**: Fluxo manual

#### MVP1 (Intermediário)

- **Tela "Delegações"**: Regional marca checkboxes por tipo de aprovação (entrada, transferência, remoção de cargo)
- **Validade**: Data início/fim
- **UI**: Tabela com clubes subordinados + switch "Delegar ao Distrital"

### Mudança de Hierarquia (Transferência entre Clubes)

#### MVP0 (Essencial)

- **Uma aprovação**: Do clube de destino (Diretor/Secretário)
- **Notificação**: Clube de origem é notificado (sem veto)

#### MVP1 (Intermediário)

- **Aprovação em cascata opcional**: Exigir aval do Distrital/Regional
- **Configurável**: Por política

---

## 🔒 Dados Sensíveis e Mascaramento

### Campos a Mascarar (MVP0)

- CPF, telefone, email não verificado, data de nascimento, endereço completo
- Dados médicos/contato de emergência, anotações disciplinares

### Níveis de Acesso

#### Diretor (do clube)

- **Pode ver**: Campos sensíveis dos membros do próprio clube

#### Secretário (do clube)

- **Pode ver**: CPF/contatos/endereço/data de nascimento
- **Não vê**: Anotações disciplinares por padrão
- **Dados médicos**: Somente leitura (sem exportar)

#### Distrital/Regional

- **Visão agregada**: Por padrão sem revelar campos sensíveis individuais
- **Apenas contagem/indicadores**: Revelação pontual pode ser delegada (MVP1)

### Auditoria do "Revelar por 10s"

#### MVP0 (Essencial)

- **Registrar**: Quem, quando e qual campo foi revelado (ID do membro, campo, requestId)

#### MVP1 (Intermediário)

- **Incluir**: Motivo (campo livre) e IP/UA
- **Relatório exportável**: Para compliance

---

## 📝 Onboarding e Validação

### Campos Mínimos para "Ativo" (MVP0)

- Nome completo
- Data de nascimento
- CPF válido (formato e dígito) + não duplicado
- Um contato verificado (email ou telefone)
- Clube e Unidade atribuídos
- Aceite de termos/consentimento (quando aplicável)

**Status "ativo" = todos os itens acima cumpridos. Caso contrário: "pendente".**

### Validação em Tempo Real

#### MVP0 (Essencial)

- **Client-side**: Validação de formato durante digitação
- **Server-side**: Em blur/submissão (CPF único, email único)

#### MVP1 (Intermediário)

- **Verificação assíncrona**: Telefone (OTP/SMS) e email (link mágico)

### Bloqueio de Ações

#### MVP0 (Essencial)

- **Não pode alocar**: Em unidade sem completar campos mínimos

#### MVP1 (Intermediário)

- **Permitir "reserva de vaga"**: Na unidade com rótulo Pendente (sem benefícios até completar)

---

## 👥 Gestão de Unidades e Alocação

### Capacidade Visual (MVP0)

- **Chip/badge**: Ocupação 12/15 + barra de progresso
- **Cores**: Verde ≤80%, amarelo 81–100%, vermelho >100%
- **Tooltip**: Faixa etária sugerida/permitida

### Alocação Automática

#### MVP0 (Essencial)

- **Sugestão baseada**: Idade (regra etária da unidade) + ocupação (priorizar menos lotadas)
- **Override manual**: Permitido

#### MVP1 (Intermediário)

- **Sugerir também**: Por gênero/cargo quando aplicável a regras internas

### Transferência entre Unidades do Mesmo Clube

#### MVP0 (Essencial)

- **Permitido**: Confirmação com resumo (origem → destino) e impacto (agenda/tarefas)

#### MVP1 (Intermediário)

- **Histórico**: De movimentações por membro

---

## 🎉 Eventos e Participações

### Elegibilidade (Visual - MVP0)

- **Badge por membro**:
  - Elegível (ativo + atende critérios de idade/sexo/cargo do evento)
  - Pendente (faltam requisitos mínimos)
  - Inelegível (fora de critérios)
- **Filtro de lista**: Por elegibilidade

### Taxas (MVP0)

- **Cálculo simples**: Taxa base do evento + opcionais (camisa, alimentação)
- **Mostrar resumo**: Por membro e total por unidade/clube (cards no topo)

#### MVP1 (Intermediário)

- **Descontos**: Por cargo/bolsa; parcelamento

### Confirmação

#### MVP0 (Essencial)

- **Diálogo de confirmação**: Por membro (ou lote) com resumo de custos
- **Notificação**: Enviar ao responsável

#### MVP1 (Intermediário)

- **Aceite digital**: Com termos do evento

---

## 📊 Relatórios e Exportação

### Permissões

#### MVP0 (Essencial)

- **Diretor e Secretário**: Exportam CSV do próprio clube
- **Distrital/Regional**: Exportam agregados (sem dados sensíveis individuais)

#### MVP1 (Intermediário)

- **Perfis superiores**: Podem solicitar extração detalhada com justificativa (auditoria)

### Filtros

#### MVP0 (Essencial)

- **Período**: Criação/atualização
- **Unidade, status**: Ativo/pendente/inelegível
- **Faixa etária**

#### MVP1 (Intermediário)

- **Cargo, elegibilidade**: Por evento
- **Flags de dados sensíveis**: Somente contagem

### Formato

#### MVP0 (Essencial)

- **Preview paginado**: Na tela (colunas definidas) + botão Exportar CSV

#### MVP1 (Intermediário)

- **Exportação assíncrona**: Com notificação quando pronto

---

## 🔔 Notificações e Feedback

### Convites

#### MVP0 (Essencial)

- **Toast**: "Convite enviado" + badge "Pendente"
- **Exibir**: Último envio e reenvio com cooldown (60s) e confirmação

#### MVP1 (Intermediário)

- **Email + opcional SMS**: Ao aceitar, marcar "Ativado" e registrar data

### Aprovações

#### MVP0 (Essencial)

- **Lista**: "Aguardando aprovação" por clube
- **Alert**: Na home do Diretor/Secretário

#### MVP1 (Intermediário)

- **Push/email**: Para perfis delegados (Distrital/Regional) conforme regras

### Erros de Validação (MVP0)

- **Inline no formulário**: "CPF já cadastrado em outro membro: [ver registro]"
- **Oferecer ação**: Ver, iniciar transferência (quando aplicável)

---

## 📱 Mobile e Responsividade

### Funcionalidades Críticas no Mobile (MVP0)

- Login, recuperação de acesso
- Listar/buscar membros; ver perfil (com máscara/"revelar")
- Criar/editar membro (campos essenciais)
- Enviar/reenviar convite
- Aprovar transferência de unidade dentro do clube

### Offline

#### MVP0 (Essencial)

- **Não suportado**

#### MVP2+ (Avançado)

- **PWA**: Rascunhos de formulários não sensíveis (IndexedDB) e leitura de listas recentes

---

## 🩹 PATCH — Respostas às Perguntas Pós-MVP0 do Backend

> **Incorporação:** anexar este bloco ao final do arquivo .mdc existente. Todas as decisões abaixo são **válidas para o MVP0** salvo indicação em contrário.

### 1) Estrutura de Dados para Frontend

**Payload de criação (referência):** usar o JSON enviado como _baseline_, porém o **MVP0** exige apenas o **mínimo**:

- **Obrigatórios (MVP0):**
  - `firstName`, `lastName`
  - `dateOfBirth` (ISO curto `YYYY-MM-DD`)
  - `cpf` (válido, não duplicado)
  - **Um contato verificado:** `loginInfo.email` **ou** `contacts[0].value` (telefone)
  - `clubId`, `unitId`
  - Aceite de termos/consentimento (quando aplicável)
- **Opcionais (não bloqueiam ativação):**
  - `middleNames`, `socialName`, `rg`
  - `addressInfo` completo
  - contatos adicionais
- **Pós-MVP0:** `baptismInfo`, `initialScarfInvestiture` e progresso espiritual.

**Validações:**

- **Frontend:** formato e máscaras (CPF, email, telefone, data), obrigatoriedade visual, idade válida, CEP.
- **Backend (autoritativo):** unicidade (CPF/email/telefone), RBAC, consistência hierárquica (clube→unidade), capacidade de unidade, regras de elegibilidade.

---

### 2) Fluxo de Onboarding

- **Status `Pending`:** exibir **banner** no perfil + **checklist** com itens obrigatórios (✅ completo / ⏳ pendente / ❌ inválido); cada item navega ao campo.
- **Checklist de ativação:** renderizar como lista clicável; mostrar progresso (%). Ao completar obrigatórios → status muda para **Active**.
- **Bloqueios de operação:** mostrar **Dialog**/tooltip padronizado quando ação for bloqueada (ex.: "Conclua CPF e contato verificado para continuar") com CTA **"Ir para checklist"**.

---

### 3) Sistema de Eventos

- **Elegibilidade visual:** badge por membro — **Elegível** (verde), **Pendente** (amarelo), **Inelegível** (cinza/vermelho). Filtro rápido por elegibilidade.
- **Participações (MVP0):** inscrição **simples** (toggle) + **resumo de taxas** (taxa base + opcionais) por membro; cards de totais por unidade/clube.
- **Pagamentos (MVP0):** registrar **intenção/compromisso** (sem gateway).
- **Filtros essenciais:** unidade, status (ativo/pendente/inelegível), faixa etária, cargo (quando aplicável).

---

### 4) Permissões e Escopo

- **Diretor/Secretário:** veem e gerenciam **apenas membros do próprio clube** (todas as unidades).
- **Distrital/Regional:** **leitura** de todos os clubes do seu escopo; **sem revelar dados sensíveis** individualmente no MVP0.
- **Delegações (Regional→Distrital):** **fora do MVP0**; entra no **MVP1** com tela de Delegações.

---

### 5) UX/UI Específicas

- **Dados sensíveis — "Revelar por 10s":** timer **automático** com contagem regressiva + ação **"Ocultar agora"**; ao expirar, volta a mascarado; **auditoria** (quem, quando, qual campo, id do membro, requestId). Motivo/IP/UA → **MVP1**.
- **Confirmações:** usar padrão global (`useConfirm`) para **exclusões**, **convites (enviar/reenviar com cooldown 60s)** e **transferência entre unidades**; foco no **Cancelar**, `Esc` cancela, **Enter não confirma**; hard-delete pede **digitar o nome**.
- **Loading states:** **spinners/linear progress** no **MVP0**; **skeleton loading** apenas no **MVP1**.

---

### 6) Integração com Backend

- **Base URL:** via env `VITE_API_BASE_URL` (dev padrão: `http://localhost:5258`).
- **CORS (dev):** permitir `http://localhost:5173`; métodos `GET,POST,PUT,PATCH,DELETE,OPTIONS`; headers `Content-Type, Authorization, X-Request-Id, Idempotency-Key`; `Credentials: true`.
- **Error handling:**
  - **401** → redirect para `/login?next=…`
  - **403** → alerta "Permissão insuficiente"
  - **404** → placeholder "Não encontrado"
  - **409** → conflito (ex.: **CPF já existe**) com ação sugerida (_ver registro_ / _iniciar transferência_)
  - **422** → exibir **erros por campo** no formulário
  - **429** → aviso de rate limit
  - **5xx** → toast "Erro inesperado" (log no Sentry a partir do MVP1)

---

### 7) Dados de Teste (Dev)

- **Estrutura hierárquica de exemplo:**
  - **Clube A** (idA): **Harpia** (cap 15), **Falcão** (cap 15)
  - **Clube B** (idB): **Águia** (cap 12)
- **Usuários de teste:**
  - Diretor_ClubeA / `dirA@example.com` / `Passw0rd!`
  - Secretario_ClubeA / `secA@example.com` / `Passw0rd!`
  - Distrital_D1 / `dist1@example.com` / `Passw0rd!` (leitura distrito)
  - Regional_R1 / `reg1@example.com` / `Passw0rd!` (leitura região)
- **Seed:** ~30 membros variados + 1 evento ativo (taxa base + opcional).

---

### 8) Observações Finais

- **Relatórios CSV (MVP0):** **apenas Diretor do Clube**; Distrital/Regional veem **agregados na UI** (sem dados sensíveis individuais).
- **Datas/Timezone:** persistir em **UTC**; exibir em **America/Sao_Paulo**; formato `dd/MM/yyyy` (e `HH:mm` quando aplicável).

---

---

## 🎯 Metodologia Estratégica de Desenvolvimento

### Abordagem Híbrida: Admin → Revisar por Nível

**Para este projeto específico, seguimos a metodologia híbrida que prioriza velocidade de desenvolvimento inicial com arquitetura flexível para escalabilidade posterior.**

#### 1. **Fase 1: Desenvolvimento Admin-First (MVP0)**

- **Foco**: Implementar funcionalidades completas para usuário **Administrador**
- **Objetivo**: MVP funcional rapidamente
- **Estratégia**: Desenvolver com arquitetura flexível desde o início

#### 2. **Fase 2: Refatoração Multi-Nível (MVP1+)**

- **Foco**: Adaptar funcionalidades para diferentes níveis de usuário
- **Objetivo**: Escalabilidade e reutilização de código
- **Estratégia**: Aproveitar arquitetura flexível criada na Fase 1

### **Por que esta abordagem para este projeto?**

#### ✅ **Vantagens Contextuais**

- **Sistema hierárquico complexo**: 8 níveis (Divisão → União → Região → Associação → Distrito → Clube → Unidade → Membro)
- **MVP0 focado**: Administrador como usuário principal inicial
- **Desenvolvimento ágil**: Funcionalidade básica primeiro, refinamento depois
- **Arquitetura pensada**: Código flexível desde o início

#### 🏗️ **Implementação Inteligente**

**Estrutura de código flexível:**

```typescript
// Hook flexível para diferentes níveis
const useMembersList = (userLevel: UserLevel) => {
  // Lógica base + customizações por nível
};

// Componente base reutilizável
const MembersList = ({ userLevel, groupingStrategy }) => {
  // Renderização base + customizações
};

// Context para gerenciar nível do usuário
const UserLevelContext = createContext<UserLevel>("admin");
```

**Padrões de desenvolvimento:**

- **Hooks customizados** com parâmetros de nível
- **Componentes base** com props de customização
- **Contextos** para gerenciar estado de usuário
- **Estratégias** para diferentes comportamentos por nível

#### 📋 **Cronograma de Implementação**

**MVP0 (Admin-First):**

1. ✅ Sistema de autenticação
2. ✅ Dashboard com dados mockados
3. 🔄 **CRUDs completos para Admin** (em andamento)
4. ⏳ Integração com APIs reais
5. ⏳ Testes end-to-end

**MVP1 (Multi-Nível):**

1. Refatoração para diferentes níveis de usuário
2. Customizações específicas por nível
3. Testes de permissões e escopo
4. Otimizações de performance

#### 🎯 **Benefícios da Abordagem**

- **Desenvolvimento mais rápido** inicialmente
- **Arquitetura escalável** desde o início
- **Menos refatoração** posterior
- **Foco em uma persona** por vez
- **MVP funcional** mais rapidamente
- **Código reutilizável** e bem estruturado

---

## 👥 Sistema de Membros - Frontend MVP0

### **Página Principal: MembersPage**

- **Rota**: `/members` (protegida, apenas autenticados)
- **Nível**: Admin (MVP0), futuramente adaptável para outros níveis
- **Layout**: Full-width com sidebar de filtros e área principal de listagem

### **Componentes Implementados**

#### **1. MemberFilters**

- **Busca textual**: Nome, CPF, email em tempo real
- **Filtros avançados**: Status, gênero, faixa etária, hierarquia
- **Filtros hierárquicos**: Divisão, União, Região, Associação, Distrito, Clube, Unidade
- **Controle de estado**: Expandir/colapsar filtros avançados
- **Contador de filtros ativos**: Badge visual com quantidade
- **Limpeza de filtros**: Botão para resetar todos os filtros

#### **2. MembersList**

- **Agrupamento flexível**: Hierárquico, por clube, por unidade, lista simples
- **Seleção múltipla**: Checkbox individual e "selecionar todos"
- **Ações por membro**: Visualizar, editar, transferir, excluir
- **Informações resumidas**: Nome, status, clube, unidade, idade, contato
- **Estados visuais**: Loading, erro, lista vazia
- **Expansão de grupos**: Collapse/expand para grupos hierárquicos

#### **3. useMembersList Hook**

- **Gerenciamento de estado**: Filtros, paginação, seleção, dados
- **Cache inteligente**: React Query com staleTime e cacheTime
- **Performance**: useMemo para cálculos pesados, useCallback para funções
- **Flexibilidade**: Suporte a diferentes níveis de usuário e estratégias
- **Estatísticas**: Cálculo em tempo real de métricas

### **Funcionalidades Implementadas**

#### **Agrupamento Hierárquico**

- **Estratégias**: Hierárquico, Por Clube, Por Unidade, Lista Simples
- **Visualização**: Cards expansíveis com contadores de membros
- **Navegação**: Breadcrumb hierárquico (Divisão > União > Região...)
- **Performance**: Lazy loading de grupos e membros

#### **Filtros e Busca**

- **Busca global**: Campo único para nome, CPF, email
- **Filtros múltiplos**: Status, gênero, idade, hierarquia
- **Filtros hierárquicos**: Dropdowns com contadores de membros
- **Persistência**: Filtros mantidos durante navegação
- **Reset**: Limpeza individual ou completa de filtros

#### **Ações e Operações**

- **CRUD completo**: Criar, visualizar, editar, excluir membros
- **Ações em massa**: Exportar, excluir, transferir selecionados
- **Transferência**: Mover membros entre clubes/unidades
- **Validação**: CPF único, email único, campos obrigatórios
- **Confirmação**: Dialogs para ações destrutivas

#### **Interface e UX**

- **Design responsivo**: Mobile-first, breakpoints Material-UI
- **Estados de loading**: Skeleton, progress bars, spinners
- **Feedback visual**: Snackbars, alerts, tooltips
- **Acessibilidade**: ARIA labels, navegação por teclado
- **Performance**: Memoização, lazy loading, virtualização

### **Tipos e Interfaces**

- **Member**: Entidade principal com todos os campos
- **MemberFilters**: Interface para filtros com validação
- **MemberGroup**: Estrutura para agrupamento hierárquico
- **UserLevel**: Enum para diferentes níveis de usuário
- **HierarchyLevel**: Enum para níveis hierárquicos
- **ContactType**: Enum para tipos de contato

### **Serviços e APIs**

- **membersService**: Classe para comunicação com backend
- **Endpoints completos**: CRUD, filtros, exportação, validação
- **Tratamento de erros**: Try/catch com feedback visual
- **Cache**: React Query para otimização de requests
- **TypeScript**: Tipagem forte em todas as interfaces

### **Performance e Otimização**

- **React.memo**: Componentes de lista para evitar re-renders
- **useMemo**: Cálculos pesados e dados estáticos
- **useCallback**: Funções passadas como props
- **Lazy loading**: Componentes carregados sob demanda
- **Paginação**: 20 itens por página com navegação

### **Próximos Passos (MVP1)**

- **Adaptação multi-nível**: Hook flexível para diferentes usuários
- **Formulários**: Criação e edição de membros
- **Relatórios**: Dashboards específicos por nível
- **Notificações**: Sistema de alertas e updates
- **Mobile**: App nativo ou PWA

---

## ⚡ Instrução para Cursor

Sempre respeitar este script como fonte de verdade para arquitetura, UX e regras de negócio ao gerar código ou componentes do frontend PathfinderManagementSoftware.
